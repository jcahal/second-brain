# SQL

Crash course on the relational query language — the core syntax, what each clause is actually doing, and the gotchas that bite when you move from toy examples to real data.

## Sheets

| Sheet | What's in it |
|---|---|
| [Core SQL](/second-brain/sql/core) | SELECT, JOINs, GROUP BY, subqueries, CTEs, window functions, set ops, NULL semantics, gotchas |
